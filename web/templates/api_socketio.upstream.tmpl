{{/* emit upstreams for running server containers */}}
{{ range $c := . }}
  {{ if and $c.State.Running (eq (index $c.Labels "com.docker.compose.service") "server") -}}
server {{ $c.Name }}:8000 max_fails=3 fail_timeout=30s;
  {{- end }}
{{ end }}
