{{/* emit upstreams for running client containers */}}
{{ range $c := . }}
  {{ if and $c.State.Running (eq (index $c.Labels "com.docker.compose.service") "client") -}}
server {{ $c.Name }}:3000 max_fails=3 fail_timeout=30s;
  {{- end }}
{{ end }}
